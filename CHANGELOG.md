# 更新日志

## v2.0.0

### 重大重构：转向动态注入式扩展包架构

- **架构重构**：
  - 由原先独立 Fork 副本模式重构为面向 `astrbot_plugin_parser` 的轻量“动态注入扩展包”。
  - 启动时自动注入扩展解析器至原插件中，复用原插件的消息分发、Downloader、Renderer、仲裁器、防抖器与缓存清理，零冗余开销。
  - 原插件若更新卡片渲染或下载机制，扩展包追加的解析器 100% 自动同步享受最新特性。
- **解析器瘦身与保留**：
  - 彻底清理原插件已有的 15+ 个重复解析器及重复核心代码。
  - 仅保留并增强：`Telegram`、`磁力链接`、`JMComic`、`网易云音乐 (NodeJS API版)`、`小红书 (原图版)`。
- **指令与联动优化**：
  - 移除与原插件重叠的指令（`开启解析`、`关闭解析`、`登录B站`），原插件的开关指令对本插件解析器完全联动生效。
  - 保留专属指令：`登录网易云`、`登录Telegram`，新增 `ext重载`。
- **配置与容错**：
  - 配置重构为原插件同款的 `template_list` 选择框，按需开启或关闭各个扩展解析器。
  - 原插件缺失时提供明确友好的控制台报错与指令拦截提示；支持原插件热重载全自动检测重新注入。
